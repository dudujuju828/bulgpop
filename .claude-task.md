You are improving BulgaPop, a Next.js (App Router) + React + TypeScript browser game for learning Bulgarian vocabulary. Bubbles fall from the top; the player pops one and types the word before it reaches the bottom.

IMPORTANT: This repo uses a modified version of Next.js (16.x). Before writing any Next.js-specific code, read the relevant guide in node_modules/next/dist/docs/. Heed deprecation notices. Read AGENTS.md.

Key files:
- components/Game.tsx + Game.module.css — all game logic, rendering, the menu, and fall mechanics
- lib/vocab.ts — vocabulary, answer matching (normalize/matches), adaptive pickIndex
- app/page.tsx, app/layout.tsx — shell
- e2e/*.spec.ts — Playwright tests (flows, logic, responsive)

YOUR TASK — implement these improvements, prioritizing the FIRST as the headline feature:

1. BUBBLE FALL-TIME CONTROL (headline). Add an explicit, user-facing setting that controls how long a bubble takes to fall from top to bottom. Today fall speed is implicitly tied to difficulty mode and ramps up. Give the player direct control:
   - A clear menu control (e.g. a labeled slider or a set of buttons) to choose the base fall duration, expressed in human terms (e.g. seconds to drop, or Relaxed / Normal / Brisk / Fast presets that map to concrete seconds).
   - Persist the choice to localStorage so it survives reloads.
   - Make sure it composes sensibly with the existing difficulty speed-ramp (it should set the BASE duration; the ramp can still make it harder over time). Consider an option to disable the ramp ("steady speed") for low-pressure learning.

2. LEARNING QUALITY-OF-LIFE features (implement a sensible, polished subset — at least 3):
   - A "Practice / Zen mode" with no lives and no time pressure (or very slow), so a learner can study calmly. Optionally show the answer hint more readily here.
   - A "Show hint" affordance (e.g. reveal first letter, or show the Cyrillic, or a brief peek) at a small score cost or freely in practice mode.
   - Slow-down / pause control during a round (pause the falling bubble) so a learner isn't punished for thinking.
   - Better post-round learning: ensure missed words are clearly listed with Latin + Cyrillic and (if available) the spoken pronunciation replayable from the recap.
   - Any other small, genuinely useful learning aids you see while in the code (e.g. an audio replay button always visible, a "I knew it / didn't know it" self-grade, configurable auto-pronounce).

CONSTRAINTS:
- Keep the existing visual style and the no-external-game-library approach (bubbles/animations/sound are hand-rolled).
- Keep TypeScript strict and pass eslint (npm run lint).
- Update the README.md to document the new options.
- Add or update Playwright tests in e2e/ to cover the new fall-time setting and at least one new learning feature. Keep existing tests passing.
- Do NOT commit; leave changes in the working tree. I will review and commit.

WORKFLOW:
1. Read AGENTS.md, README.md, components/Game.tsx, components/Game.module.css, lib/vocab.ts to understand current mechanics (especially how fall speed/duration and the difficulty ramp work now).
2. Implement feature 1 fully, then the learning QoL subset.
3. Run `npm run lint` and fix all issues.
4. Run `npm test` (Playwright). It builds and serves automatically. Fix failures you introduced. If the full suite is slow, you may run a focused subset first, but ensure the whole suite passes before finishing.
5. Finish with a concise summary: what you added, which files changed, how the new settings work, and the test/lint results.
